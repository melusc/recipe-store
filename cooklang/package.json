{
	"type": "module",
	"name": "cooklang",
	"private": true,
	"exports": {
		".": "./dist/src/index.js",
		"./wasm": "./dist/wasm/cooklang_wasm.cjs"
	},
	"imports": {
		"#wasm": "./dist/wasm/cooklang_wasm.cjs"
	},
	"engines": {
		"node": "^24.8.0"
	},
	"scripts": {
		"build": "pnpm clean && pnpm build:wasm && tsc",
		"build:wasm": "wasm-pack build --release --no-pack --target nodejs --out-dir dist/wasm && pnpm postbuild:wasm",
		"postbuild:wasm": "node scripts/postbuild-wasm.ts",
		"clean": "rimraf dist",
		"fmt": "cargo fmt && eslint --fix",
		"test": "vitest run"
	},
	"license": "MIT",
	"devDependencies": {
		"@lusc/eslint-config": "^8.0.1",
		"@lusc/tsconfig": "^7.1.0",
		"@types/node": "^24.5.2",
		"eslint": "^9.36.0",
		"rimraf": "^6.0.1",
		"typescript": "^5.9.2",
		"vitest": "^4.0.3",
		"wasm-pack": "^0.13.1"
	},
	"dependencies": {
		"zod": "^4.1.11"
	}
}
