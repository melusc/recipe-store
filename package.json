{
	"type": "module",
	"packageManager": "pnpm@11.15.1",
	"devDependencies": {
		"prettier": "^3.9.6"
	},
	"private": true,
	"scripts": {
		"fmt": "prettier -w . && pnpm -r fmt",
		"build": "pnpm -r build",
		"test": "pnpm -r test",
		"start": "pnpm --filter backend start",
		"dev": "pnpm build && pnpm start"
	},
	"engines": {
		"node": "^24.15.0 || ^26.0.0"
	},
	"license": "GPL-3.0-only"
}
